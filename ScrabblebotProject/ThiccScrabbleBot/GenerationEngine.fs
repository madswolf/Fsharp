module GenerationEngine

let findHorisontalAndVeriticalAffixes map dict =
    //find Horisontal prefixes 

    //Find Vertical prefixes

    //prefix = ( ( (char * coord) list * horisontal(bool) ) * prefix/suffix(bool) ) 

    //prefixes = prefix list

    ([(( [('H',(0,0))], false ) , false)])

let generateAWordBasedOnHandAndPrefixes prefixes hand =
    
    //run through all prefixes and try each combination of tiles in hand 

    //See if combination is word is true

    //See if play is valid

    //if valid return that move

    //(word,horisontal)
    (false)